//TODO Без форка скопировать ssh-проекта
//NOTE Кто-то один клонирует себе на машину репозиторий git clone git@github.com:Elbrus-Bootcamp/project-sudoku.git (Клонирование в «project-sudoku»...)
//TODO Открываем проект в vs-code и удаляем связь с внешним репозиторием (rm -rf .git)
//NOTE Инициализируем новый локальный репозиторий git init
//TODO Добавляем в отслеживание файлы и коммитим git add ./ && git commit -m 'initial'
//NOTE В гите создаем новый личный пустой приватный репозиторий
//TODO Копируем второй пул команд из предложенных после создания
//* git remote add origin <имя вашего репозитория>
//* git branch -M main
//* git push -u origin main
//NOTE Переходим в гит и рассылаем инвайты своим ребятам)
//TODO После того, как инвайт принят, колобораторы могут клонировать по ssh себе на машинку проект и работать по разным веткам

//FIX ОДНА ФИЧА - ОДНА ВЕТКА
//FIX ФИЧУ ЗАЛИЛИ В DEV - удалили ветку удаленно и локально и ушли в новую (не забудьте выйти в dev и подтянуть слитые изменения  - git pull)

//* ВСЕ КОМАНДЫ ЕСТЬ В ПРЕЗЕНТАЦИИ
